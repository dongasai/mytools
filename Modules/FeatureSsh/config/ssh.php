<?php

declare(strict_types=1);

/**
 * FeatureSsh模块SSH配置
 *
 * @author AI开发团队
 * @date 2026-09-02
 */

return [
    /*
    |--------------------------------------------------------------------------
    | SSH默认配置
    |--------------------------------------------------------------------------
    |
    | 这些配置项定义了SSH连接的默认行为。
    |
    */

    'default' => [
        'timeout' => 30,
        'port' => 22,
    ],

    /*
    |--------------------------------------------------------------------------
    | 连接池配置
    |--------------------------------------------------------------------------
    |
    | 连接池用于复用SSH连接，减少重复建立连接的开销。
    |
    */

    'connection_pool' => [
        'enabled' => true,
        'max_connections' => 10,
        'max_per_server' => 3,
        'idle_timeout' => 300,
    ],

    /*
    |--------------------------------------------------------------------------
    | 命令执行配置
    |--------------------------------------------------------------------------
    |
    | 命令执行的默认配置。
    |
    */

    'command' => [
        'default_timeout' => 30,
        'max_timeout' => 3600,
        'output_limit' => 10000,
    ],

    /*
    |--------------------------------------------------------------------------
    | SFTP配置
    |--------------------------------------------------------------------------
    |
    | SFTP文件传输的配置。
    |
    */

    'sftp' => [
        'chunk_size' => 8192,
        'transfer_timeout' => 300,
    ],

    /*
    |--------------------------------------------------------------------------
    | 密钥配置
    |--------------------------------------------------------------------------
    |
    | SSH密钥生成和管理的配置。
    |
    */

    'keys' => [
        'default_type' => 'ed25519',
        'rsa_bits' => 4096,
        'ecdsa_curve' => 'secp384r1',
    ],

    /*
    |--------------------------------------------------------------------------
    | 日志配置
    |--------------------------------------------------------------------------
    |
    | 命令执行日志的配置。
    |
    */

    'logging' => [
        'enabled' => true,
        'retention_days' => 30,
        'max_output_length' => 10000,
    ],
];
